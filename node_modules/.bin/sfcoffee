#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../node-salesforce/bin/sfcoffee" "$@"
  ret=$?
else 
  node  "$basedir/../node-salesforce/bin/sfcoffee" "$@"
  ret=$?
fi
exit $ret
